const state={db:null,view:'command',captureType:'task'};
const $=s=>document.querySelector(s); const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const offlineState={constitution:{values:['Clarity','Craft','Relationships'],activeLimit:3},tasks:[{id:'offline-1',title:'Review Ourex architecture',project:'Ourex',priority:'high',status:'in-progress',estimate:90,due:'Today',energy:'deep'},{id:'offline-2',title:'Send follow-up to Sara',project:'Relationships',priority:'medium',status:'open',estimate:15,due:'Today',energy:'light'},{id:'offline-3',title:'Synthesize MCP research',project:'Ourex',priority:'high',status:'open',estimate:60,due:'Tomorrow',energy:'deep'}],projects:[{id:'p1',name:'Ourex',description:'Personal OS architecture',progress:68,risk:31,clarity:82,momentum:74,next:'Resolve integration boundary',status:'active'}],people:[{id:'r1',name:'Sara Rahimi',role:'Product collaborator',lastContact:'6 days ago',importance:'high',need:'Follow up on Ourex research'}],ideas:[{id:'i1',title:'MCP as a universal connection layer',summary:'Connect Ourex to external AI systems without coupling the core.',status:'developing',potential:'high'}],insights:[{id:'n1',kind:'Focus',title:'Your active portfolio is within your preferred limit',body:'Three active projects match your constitution. Protect the next deep-work window for Ourex.',confidence:.91},{id:'n2',kind:'Risk',title:'Home studio needs a decision',body:'Clarity is low and momentum has fallen. A 20-minute scope decision can unblock it.',confidence:.78}]};
async function load(){try{const r=await fetch('/api/state',{headers:{Accept:'application/json'}});if(!r.ok)throw Error('API unavailable');state.db=await r.json()}catch(e){state.db=JSON.parse(JSON.stringify(offlineState));state.offline=true}render()}
function shell(title,kicker='WORKSPACE'){ $('#view-title').textContent=title;$('#view-kicker').textContent=kicker; }
function render(){const d=state.db; if(state.view==='command') return command(d); const titles={projects:'Projects',tasks:'Tasks',people:'Relationships',ideas:'Idea garden',research:'Research & knowledge',reviews:'Reviews',memory:'Memory governance',agents:'Agents & skills',settings:'System control'}; shell(titles[state.view]||'Command Center',state.view.toUpperCase()); const map={projects:projects,tasks:tasks,people:people,ideas:ideas,research:research,reviews:reviews,memory:memory,agents:agents,settings:settings}; $('#app').innerHTML=map[state.view]?map[state.view](d):''}
function command(d){shell('What matters now?','COMMAND CENTER'); $('#app').innerHTML=`<div class="grid"><section class="card"><div class="card-head"><div><div class="eyebrow">TODAY · TUESDAY, AUGUST 25</div><h2 style="margin-top:7px">Your attention, intentionally allocated.</h2></div><button class="primary" id="capture">＋ Capture</button></div><div class="objective"><div class="orb">✦</div><div><span class="label">MAIN OBJECTIVE</span><strong>Ourex architecture review</strong><small>Deep work · 90 min · supports your quarterly goal</small></div></div><div class="card-head"><div><h2>Top priorities</h2><span class="muted">3 meaningful moves for today</span></div><button class="quiet" data-view="tasks">View all →</button></div>${d.tasks.filter(t=>t.due==='Today').map(taskHTML).join('')}</section><section class="card"><div class="card-head"><div><div class="eyebrow">EXECUTIVE SIGNAL</div><h2 style="margin-top:7px">What your OS sees</h2></div><span class="pill">Live context</span></div>${d.insights.map(i=>`<div class="insight ${i.kind==='Risk'?'risk':''}"><div class="dot"></div><div><strong>${esc(i.title)}</strong><p>${esc(i.body)}</p><span class="confidence">Confidence ${Math.round(i.confidence*100)}%</span></div></div>`).join('')}<div style="margin-top:16px"><button class="secondary" data-view="reviews">Open weekly review →</button></div></section><section class="card"><div class="card-head"><div><h2>Today's rhythm</h2><span class="muted">Space protected for what matters</span></div><button class="quiet">Calendar ↗</button></div><div class="schedule"><div class="event"><time>10:00</time><div><strong>Architecture review</strong><small>Ourex · Deep work</small></div></div><div class="event"><time>14:00</time><div><strong>Open space</strong><small>Protected focus window · 90 min</small></div></div><div class="event"><time>17:30</time><div><strong>Evening review</strong><small>Close the loop · 10 min</small></div></div></div><div class="objective" style="margin:22px 0 0;background:#fff8ef"><div class="orb" style="background:#c88945">◷</div><div><span class="label">CAPACITY CHECK</span><strong>3h 20m unallocated</strong><small>Your day has room. Don't fill it prematurely.</small></div></div></section><section class="card"><div class="card-head"><div><h2>People needing attention</h2><span class="muted">Based on importance and recent context</span></div><button class="quiet" data-view="people">See relationships →</button></div>${d.people.map(p=>`<div class="people"><div class="avatar">${esc(p.name[0])}</div><div><strong>${esc(p.name)}</strong><small>${esc(p.need)} · ${esc(p.lastContact)}</small></div><button class="quiet" style="margin-left:auto">Follow up</button></div>`).join('')}</section></div>`; bindCommon()}
function taskHTML(t){return `<div class="task"><div class="check ${t.status==='done'?'done':''}" data-task="${t.id}">${t.status==='done'?'✓':''}</div><div><div class="title">${esc(t.title)}</div><div class="meta">${esc(t.project)} · ${t.estimate} min · ${esc(t.energy)} energy</div></div><span class="tag">${esc(t.priority)}</span></div>`}
function projects(d){return `<div class="card full"><div class="card-head"><div><h2>Project portfolio</h2><span class="muted">Health is explained, not just colored.</span></div><button class="primary" id="capture">＋ New project</button></div><div class="view-list">${d.projects.map(p=>`<article class="entity"><div class="entity-top"><div><h3>${esc(p.name)}</h3><span class="muted">${esc(p.description)}</span></div><span class="pill">${p.status}</span></div><div class="bar-row"><div class="bar-label"><span>Progress</span><b>${p.progress}%</b></div><div class="bar"><i style="width:${p.progress}%"></i></div></div><div class="bar-row"><div class="bar-label"><span>Clarity</span><b>${p.clarity}%</b></div><div class="bar"><i style="width:${p.clarity}%"></i></div></div><small class="muted">Next action: <b>${esc(p.next)}</b> · Risk ${p.risk}% · Momentum ${p.momentum}%</small></article>`).join('')}</div></div>`}
function tasks(d){return `<div class="card full"><div class="card-head"><div><h2>Open loops</h2><span class="muted">Prioritized by strategic value, urgency and capacity.</span></div><button class="primary" id="capture">＋ Add task</button></div>${d.tasks.map(taskHTML).join('')}</div>`}
function people(d){return `<div class="card full"><div class="card-head"><div><h2>Relationship intelligence</h2><span class="muted">Attention is care, not a score.</span></div><button class="primary" id="capture">＋ Log interaction</button></div><div class="view-list">${d.people.map(p=>`<article class="entity"><div class="entity-top"><div><h3>${esc(p.name)}</h3><span class="muted">${esc(p.role)}</span></div><span class="pill">${esc(p.importance)} importance</span></div><p class="muted">${esc(p.need)} · Last meaningful contact ${esc(p.lastContact)}</p><button class="secondary">Plan follow-up</button></article>`).join('')}</div></div>`}
function ideas(d){return `<div class="card full"><div class="card-head"><div><h2>Idea garden</h2><span class="muted">Capture freely. Develop deliberately.</span></div><button class="primary" id="capture">＋ Plant an idea</button></div><div class="view-list">${d.ideas.map(i=>`<article class="entity"><div class="entity-top"><h3>✦ ${esc(i.title)}</h3><span class="pill">${i.status}</span></div><p class="muted">${esc(i.summary)}</p><small>Potential: <b>${i.potential}</b> · Connected to Ourex</small></article>`).join('')}</div></div>`}
function research(){return `<div class="grid"><section class="card"><div class="eyebrow">RESEARCH OS</div><h2 style="margin-top:8px">Continue from what you know.</h2><p class="muted">Before you start new research, Ourex searches prior findings, checks what has gone stale and shows what remains unknown.</p><button class="primary" id="capture">＋ Start research</button></section><section class="card"><h2>Knowledge trail</h2><p class="muted">MCP architecture · 12 sources · 4 claims · updated today</p><div class="insight"><div class="dot"></div><div><strong>2 unresolved questions</strong><p>Both block the current architecture decision.</p></div></div></section></div>`}
function reviews(){return `<div class="grid"><section class="card"><div class="eyebrow">WEEKLY EXECUTIVE REVIEW</div><h2 style="margin:8px 0">A clear week starts here.</h2><p class="muted">Wins, stalls, risks, relationships and next week's strategy — in one concise review.</p><button class="primary">Generate review</button></section><section class="card"><h2>Monthly state of life</h2><p class="muted">Your next monthly review will be available in 5 days.</p><div class="bar-row"><div class="bar-label"><span>Cognitive offloading</span><b>74%</b></div><div class="bar"><i style="width:74%"></i></div></div></section></div>`}
function memory(){return `<div class="card full"><div class="card-head"><div><h2>Memory governance</h2><span class="muted">You own what Ourex remembers.</span></div><button class="secondary">Export memory</button></div><div class="view-list">${['Identity memory · 8 items','Preference memory · 14 items','Project memory · 32 items','Relationship memory · 21 items','Decision memory · 9 items'].map(x=>`<article class="entity"><div class="entity-top"><h3>◈ ${x.split(' · ')[0]}</h3><span class="pill">Governed</span></div><small class="muted">Inspect · edit · delete · Why do you remember this?</small></article>`).join('')}</div></div>`}
function agents(){return `<div class="card full"><div class="card-head"><div><h2>Agents & skills</h2><span class="muted">Specialized capabilities, one shared context.</span></div><button class="primary">＋ Create skill</button></div>${['Chief of Staff','Project Agent','Research Agent','Relationship Agent','Learning Agent','Decision Agent'].map((x,i)=>`<article class="entity" style="display:inline-block;width:31%;margin:1%;vertical-align:top"><h3>◎ ${x}</h3><p class="muted">Shared memory · permission-aware · active</p><span class="pill">${i?'Ready':'Orchestrating'}</span></article>`).join('')}</div>`}
function settings(){return `<div class="grid"><section class="card"><h2>Automation level</h2><p class="muted">Default is Assistant. Higher autonomy always stays within explicit permissions.</p>${['Observer','Assistant','Delegate','Autopilot'].map((x,i)=>`<div class="people"><div class="check ${i===1?'done':''}">${i===1?'✓':''}</div><div><strong>${x}</strong><small>${['Observe and recommend only','Perform low-risk actions','Run approved workflows','Strict predefined policies'][i]}</small></div></div>`).join('')}</section><section class="card"><h2>System health</h2><div class="bar-row"><div class="bar-label"><span>Core API</span><b style="color:var(--accent)">Operational</b></div><div class="bar"><i style="width:100%"></i></div></div><div class="bar-row"><div class="bar-label"><span>Event queue</span><b style="color:var(--accent)">Healthy</b></div><div class="bar"><i style="width:94%"></i></div></div><p class="muted">Audit log · Integrations · Data export · Backup</p></section></div>`}
function bindCommon(){document.querySelectorAll('[data-view]').forEach(e=>e.onclick=()=>{state.view=e.dataset.view;render()});document.querySelectorAll('[data-task]').forEach(e=>e.onclick=async()=>{const t=state.db.tasks.find(x=>x.id===e.dataset.task);t.status=t.status==='done'?'open':'done';try{await fetch('/api/tasks/'+t.id,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:t.status})})}catch(e){}toast(t.status==='done'?'Completed and remembered':'Reopened');render()});$('#capture')?.addEventListener('click',openCapture)}
function openCapture(){$('#captureModal').classList.remove('hidden');$('#captureText').focus()}function toast(s){const t=$('#toast');t.textContent=s;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}
$('#closeModal').onclick=()=>$('#captureModal').classList.add('hidden');document.querySelectorAll('.capture-types button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.capture-types button').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');state.captureType=b.dataset.type});$('#saveCapture').onclick=async()=>{const text=$('#captureText').value.trim();if(!text)return;const item={id:'local-'+Date.now(),title:text,project:'Inbox',priority:'medium',status:'open',estimate:15,due:'Today',energy:'light',summary:'Captured moments ago',potential:'unknown'};if(state.captureType==='task')state.db.tasks.unshift(item);if(state.captureType==='idea')state.db.ideas.unshift({...item,status:'captured'});try{await fetch('/api/capture',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({entity:state.captureType,title:text,project:'Inbox',priority:'medium',estimate:15,due:'Today',energy:'light',summary:'Captured moments ago',potential:'unknown'})})}catch(e){}$('#captureText').value='';$('#captureModal').classList.add('hidden');toast('Captured. Ourex will connect the context.');render()};$('#theme').onclick=()=>document.body.classList.toggle('dark');$('#searchBtn').onclick=()=>{const q=prompt('Search your Personal OS');if(q)toast('Searching across your graph for “'+q+'”…')};document.addEventListener('click',e=>{const v=e.target.closest('[data-view]');if(v&&v.closest('#app')){state.view=v.dataset.view;render()}});load();
